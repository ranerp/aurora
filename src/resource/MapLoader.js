if (typeof(AURORA) === "undefined") var AURORA = {};

AURORA.MapLoader = function(resourceManager) {
    this.resourceManager = resourceManager;
};

AURORA.MapLoader.prototype = {

    constructor: AURORA.MapLoader,

    loadMap: function(data) {

    }
};